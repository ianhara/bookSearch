{
    "name": "book",
    "version": "1.0.0",
    "scripts": {
      "build":"cd client && npm run build && cp -r ./build ../server/",
      "server": "cd server && npm run watch",
      "client": "cd client && npm run start",
      "dev": "concurrently \"npm run server\" \"npm run client\"",
      "start":"cd server && node server.js"
    },
    "devDependencies": {
      "concurrently": "^6.2.2"
    }
  }
  